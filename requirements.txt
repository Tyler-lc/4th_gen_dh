# Pip-only fallback install list.
#
# The primary, supported install path is conda via environment.yml
# (geospatial and solver binaries install cleanly from conda-forge).
# This file exists for users who cannot use conda; on such systems
# geopandas / osmnx / gurobipy may require system-level build tools.
#
# Equivalent to the `dependencies` block in pyproject.toml plus dev extras.
# Prefer `pip install -e .[dev]` if possible — it reads pyproject.toml
# directly and will not drift from this file.

geopandas>=1.0
gurobipy>=12.0
haversine>=2.9
matplotlib>=3.9
networkx>=3.3
numpy>=1.26,<2.0
numpy-financial>=1.0
openpyxl>=3.1
osmnx>=2.0
pandas>=2.2
pvlib>=0.11
pyarrow>=17.0
pyomo>=6.9
scipy>=1.14
seaborn>=0.13
shapely>=2.0
tqdm>=4.66

# Dev
pytest>=7.0
